import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HandlerAlerts';

export default defineMessages({
  erorrConn: { id: `${scope}.erorrConn` },
  erorrConnDescription: { id: `${scope}.erorrConnDescription` },
});
