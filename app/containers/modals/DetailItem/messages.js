import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DetailItem';

export default defineMessages({
  header: { id: `${scope}.header` },
  characters: { id: `${scope}.characters` },
  teams: { id: `${scope}.teams` },
  locations: { id: `${scope}.locations` },
  concepts: { id: `${scope}.concepts` },
  withoutName: { id: `${scope}.withoutName` },
});
